# Dreamecho visionOS 应用界面设计规范文档

## 📋 目录
1. [应用概览](#应用概览)
2. [设计系统](#设计系统)
3. [界面详细描述](#界面详细描述)
4. [交互逻辑](#交互逻辑)
5. [状态管理](#状态管理)
6. [动画规范](#动画规范)

---

## 应用概览

**应用名称**: Dreamecho  
**平台**: Apple Vision Pro (visionOS)  
**设计语言**: Liquid Glass (液态玻璃)  
**主题**: 浅色主题，支持深色模式自适应

### 核心功能
- 记录梦境（文本/语音输入）
- AI 分析梦境（关键词、情感、象征、解读）
- 生成 3D 模型
- 沉浸式 3D 预览和交互

---

## 设计系统

### 液态玻璃效果 (Liquid Glass)
- **材质层级**: `.ultraThinMaterial`, `.regularMaterial`, `.thickMaterial`
- **动态光影**: 悬停时增强阴影和高光
- **边框高光**: 渐变边框模拟玻璃边缘
- **环境适应**: 根据 `colorScheme` 自动调整

### 颜色系统
- **背景**: 白色渐变 (浅色模式) / 深灰渐变 (深色模式)
- **主色**: 蓝色到紫色渐变
- **状态色**: 
  - 成功: 绿色
  - 警告: 蓝色
  - 错误: 红色
  - 中性: 灰色

### 字体系统
- **大标题**: 34pt, Bold
- **标题**: 28pt, Bold
- **副标题**: 22pt, Semibold
- **正文**: 17pt, Regular
- **说明文字**: 15pt, Regular
- **标注**: 12pt, Regular

### 间距系统
- **卡片内边距**: 24pt
- **元素间距**: 16-32pt
- **页面边距**: 32pt

---

## 界面详细描述

### 1. 主界面 (ContentView)

#### 布局结构
```
┌─────────────────────────────────────┐
│  导航栏 (LiquidGlassNavigationBar) │
├─────────────────────────────────────┤
│                                     │
│      内容区域 (动态切换)            │
│                                     │
│  - Record View (记录界面)          │
│  - Dreams View (列表界面)          │
│                                     │
└─────────────────────────────────────┘
```

#### 导航栏组件
**位置**: 顶部固定  
**材质**: `.ultraThinMaterial`  
**高度**: 自动高度 + 16pt 垂直内边距

**导航项**:
1. **Record** (记录)
   - 图标: `plus.circle.fill`
   - 状态: 选中/未选中
   - 选中效果: 胶囊背景 + 阴影

2. **Dreams** (梦境列表)
   - 图标: `moon.stars.fill`
   - 状态: 选中/未选中
   - 选中效果: 胶囊背景 + 阴影

**交互逻辑**:
- 点击导航项切换内容区域
- 选中项有 Spring 动画过渡 (response: 0.3, dampingFraction: 0.7)
- 图标有 Bounce 动画效果

**分隔线**: 
- 位置: 导航栏下方
- 样式: Divider, 透明度 0.2

---

### 2. 记录梦境界面 (DreamInputView)

#### 布局结构
```
┌─────────────────────────────────────┐
│          Header Section             │
│  - 图标 (moon.zzz.fill)             │
│  - 标题 "Record Your Dream"        │
│  - 副标题                           │
├─────────────────────────────────────┤
│          Form Section               │
│  - Title 输入框                     │
│  - Description 文本编辑器           │
│  - Voice Input 按钮                 │
│  - 录音状态显示 (条件显示)          │
├─────────────────────────────────────┤
│          Action Section             │
│  - Generate Dream Model 按钮        │
│  - Progress View (条件显示)         │
│  - Error Message (条件显示)         │
└─────────────────────────────────────┘
```

#### Header Section
**图标**:
- SF Symbol: `moon.zzz.fill`
- 大小: 56pt
- 颜色: 蓝色到紫色渐变
- 动画: Pulse 重复动画

**标题**:
- 文本: "Record Your Dream"
- 字体: DesignSystem.title (28pt, Bold)
- 颜色: `.primary`

**副标题**:
- 文本: "Transform your dreams into 3D models"
- 字体: DesignSystem.subheadline (15pt, Regular)
- 颜色: `.secondary`

**间距**: Header 顶部 40pt, 元素间距 12pt

#### Form Section

**1. Title 输入框 (LiquidGlassTextField)**
- **标签**: "Title"
- **图标**: `text.badge.plus`
- **占位符**: "Enter dream title"
- **材质**: `.ultraThinMaterial`
- **圆角**: 12pt
- **内边距**: 16pt
- **焦点状态**: 蓝色边框高光 (2pt)
- **字体**: 17pt, Regular
- **绑定**: `@State private var title`

**2. Description 文本编辑器 (LiquidGlassTextEditor)**
- **标签**: "Description"
- **图标**: `text.alignleft`
- **占位符**: "Describe your dream in detail..."
- **最小高度**: 180pt
- **材质**: `.ultraThinMaterial`
- **圆角**: 12pt
- **内边距**: 12pt
- **焦点状态**: 蓝色边框高光 (2pt)
- **字体**: 17pt, Regular
- **绑定**: `@State private var description`

**3. Voice Input 按钮**
- **图标**: `mic` / `mic.fill` (根据状态)
- **文本**: "Voice Input" / "Stop Recording"
- **状态**: 
  - 未录音: 灰色文字
  - 录音中: 主色文字 + Pulse 动画
- **材质**: `.ultraThinMaterial`
- **圆角**: 12pt
- **内边距**: 16pt 垂直
- **宽度**: 全宽
- **交互**: 点击开始/停止录音

**4. 录音状态显示 (条件显示)**
- **显示条件**: `isRecording == true`
- **图标**: `waveform`
- **文本**: 
  - 空: "Listening..."
  - 有内容: 显示转录文本
- **材质**: `.ultraThinMaterial`
- **圆角**: 12pt
- **内边距**: 16pt
- **字体**: 15pt, Regular

#### Action Section

**1. Generate Dream Model 按钮 (LiquidGlassButton)**
- **样式**: `.primary`
- **图标**: `sparkles`
- **文本**: "Generate Dream Model"
- **启用条件**: `!title.isEmpty && !description.isEmpty && !dreamStore.isLoading`
- **禁用状态**: 灰色文字 + `.ultraThinMaterial`
- **启用状态**: 主色文字 + `.regularMaterial`
- **动画**: 
  - 悬停: 阴影增强 + 高光
  - 按压: 缩放 0.98
- **交互**: 点击创建梦境并开始处理

**2. Progress View (条件显示)**
- **显示条件**: `dreamStore.isLoading == true && currentDream != nil`
- **组件**: `DreamProgressView`
- **内容**: 
  - 圆形进度条
  - 状态图标
  - 状态文本
  - 进度消息

**3. Error Message (条件显示)**
- **显示条件**: `dreamStore.errorMessage != nil && !errorMessage.isEmpty`
- **组件**: `LiquidGlassCard`
- **内容**:
  - 错误图标 (`exclamationmark.triangle.fill`, 红色)
  - 错误标题: "Error"
  - 错误详情
  - 关闭按钮 (`xmark.circle.fill`)
- **交互**: 点击关闭按钮清除错误

#### 状态逻辑

**创建梦境流程**:
1. 用户输入标题和描述
2. 点击 "Generate Dream Model"
3. 清空输入框
4. 创建 Dream 对象
5. 开始异步处理:
   - 状态: `.analyzing` (30%)
   - 调用 API 分析梦境
   - 状态: `.generating` (70%)
   - 生成 3D 模型
   - 状态: `.completed` (100%) 或 `.failed` (0%)

**语音输入流程**:
1. 点击 "Voice Input" 按钮
2. 请求麦克风权限
3. 开始录音 (`isRecording = true`)
4. 实时显示转录文本
5. 点击 "Stop Recording" 停止
6. 转录文本填充到 Description 字段

---

### 3. 梦境列表界面 (DreamListView)

#### 布局结构
```
┌─────────────────────────────────────┐
│      NavigationStack                │
│  ┌───────────────────────────────┐ │
│  │   ScrollView                  │ │
│  │   ┌─────────────────────────┐ │ │
│  │   │  Empty State (条件)     │ │ │
│  │   │  或                      │ │ │
│  │   │  DreamCard × N          │ │ │
│  │   └─────────────────────────┘ │ │
│  └───────────────────────────────┘ │
└─────────────────────────────────────┘
```

#### Empty State (空状态)
**显示条件**: `dreamStore.dreams.isEmpty`

**内容**:
- **图标**: `moon.stars` (64pt)
- **颜色**: 蓝色到紫色渐变，透明度 0.6
- **动画**: Pulse 重复动画
- **标题**: "No Dreams Yet" (22pt, Semibold)
- **副标题**: "Record your first dream to get started" (17pt, Regular)
- **容器**: `LiquidGlassCard`, 内边距 60pt

#### DreamCard (梦境卡片)

**布局**:
```
┌─────────────────────────────────────┐
│  [状态图标] 标题          [状态徽章] │
├─────────────────────────────────────┤
│  描述文本 (最多3行)                 │
├─────────────────────────────────────┤
│  [标签图标] X keywords              │
│  [心形图标] X emotions              │
├─────────────────────────────────────┤
│  [关键词标签] [关键词标签] ...      │
├─────────────────────────────────────┤
│  [时钟] 相对时间    [3D图标] 3D Model│
└─────────────────────────────────────┘
```

**Header 区域**:
- **状态图标**: 
  - Draft: `doc.text` (灰色)
  - Analyzing: `brain.head.profile` (蓝色)
  - Generating: `sparkles` (蓝色)
  - Completed: `checkmark.circle.fill` (绿色)
  - Failed: `exclamationmark.triangle.fill` (红色)
- **标题**: Dream.title (22pt, Semibold)
- **状态徽章**: `StatusBadge` 组件

**描述区域**:
- **文本**: Dream.description
- **字体**: 17pt, Regular
- **颜色**: `.secondary`
- **行数**: 最多 3 行

**分析预览区域** (条件显示):
- **显示条件**: `dream.analysis != nil`
- **关键词计数**: `[tag.fill] X keywords`
- **情感计数**: `[heart.fill] X emotions` (粉色)

**关键词标签区域** (条件显示):
- **显示条件**: `!dream.keywords.isEmpty`
- **布局**: FlowLayout (流式布局)
- **显示数量**: 最多 5 个
- **组件**: `LiquidGlassTag`

**Footer 区域**:
- **左侧**: `[clock] 相对时间` (12pt, `.tertiary`)
- **右侧**: `[cube.transparent.fill] 3D Model` (条件显示, 14pt, `.secondary`)

**交互**:
- **点击卡片**: 导航到 `DreamDetailView`
- **悬停效果**: 卡片阴影增强

**状态徽章 (StatusBadge)**:
- **背景**: 状态色，透明度 0.15
- **文字**: 状态色
- **圆角**: 8pt
- **内边距**: 12pt 水平, 6pt 垂直

---

### 4. 梦境详情界面 (DreamDetailView)

#### 布局结构
```
┌─────────────────────────────────────┐
│      ScrollView                     │
│  ┌───────────────────────────────┐ │
│  │  Header                        │ │
│  │  - 标题                        │ │
│  │  - 创建日期                    │ │
│  ├───────────────────────────────┤ │
│  │  Description Card              │ │
│  ├───────────────────────────────┤ │
│  │  Keywords Card (条件)         │ │
│  ├───────────────────────────────┤ │
│  │  Emotions Card (条件)          │ │
│  ├───────────────────────────────┤ │
│  │  Symbols Card (条件)           │ │
│  ├───────────────────────────────┤ │
│  │  Visual Description Card (条件)│
│  ├───────────────────────────────┤ │
│  │  Interpretation Card (条件)    │ │
│  ├───────────────────────────────┤ │
│  │  View in 3D Space Button (条件)│
│  └───────────────────────────────┘ │
└─────────────────────────────────────┘
```

#### Header
- **标题**: Dream.title (28pt, Bold)
- **日期**: Dream.createdAt (15pt, Regular, `.secondary`)
- **间距**: 顶部 20pt, 元素间距 12pt

#### Description Card
- **标签**: "Dream Description" + `text.alignleft` 图标
- **内容**: Dream.description (17pt, Regular)
- **容器**: `LiquidGlassCard`

#### Keywords Card (条件显示)
- **显示条件**: `analysis.keywords.isEmpty == false`
- **标签**: "Keywords" + `tag.fill` 图标
- **内容**: FlowLayout 标签列表
- **标签样式**: `LiquidGlassTag` with `tag.fill` icon

#### Emotions Card (条件显示)
- **显示条件**: `analysis.emotions.isEmpty == false`
- **标签**: "Emotions" + `heart.fill` 图标
- **内容**: FlowLayout 标签列表
- **标签样式**: `LiquidGlassTag` with `heart.fill` icon, 粉色文字

#### Symbols Card (条件显示)
- **显示条件**: `analysis.symbols.isEmpty == false`
- **标签**: "Symbols" + `sparkles` 图标
- **内容**: FlowLayout 标签列表
- **标签样式**: `LiquidGlassTag` with `sparkles` icon

#### Visual Description Card (条件显示)
- **显示条件**: `analysis.visualDescription.isEmpty == false`
- **标签**: "Visual Description" + `eye.fill` 图标
- **内容**: 文本 (17pt, Regular)

#### Interpretation Card (条件显示)
- **显示条件**: `analysis.interpretation.isEmpty == false`
- **标签**: "Interpretation" + `brain.head.profile` 图标
- **内容**: 文本 (17pt, Regular)

#### View in 3D Space Button (条件显示)
- **显示条件**: `dream.modelURL != nil`
- **样式**: `.primary`
- **图标**: `cube.transparent.fill`
- **文本**: "View in 3D Space"
- **交互**: 点击打开沉浸式空间
- **无障碍**: 
  - Label: "View dream in 3D immersive space"
  - Hint: "Opens a full immersive 3D view of your dream model"

**卡片间距**: 24pt  
**页面边距**: 32pt

---

### 5. 进度视图 (DreamProgressView)

#### 布局结构
```
┌─────────────────────────────────────┐
│      LiquidGlassCard                 │
│  ┌───────────────────────────────┐ │
│  │  圆形进度条 (100×100pt)       │ │
│  │  ┌─────────────────────────┐ │ │
│  │  │    状态图标 (32pt)      │ │ │
│  │  └─────────────────────────┘ │ │
│  ├───────────────────────────────┤ │
│  │  状态文本 (17pt, Semibold)   │ │
│  ├───────────────────────────────┤ │
│  │  进度消息 (17pt, Regular)    │ │
│  └───────────────────────────────┘ │
└─────────────────────────────────────┘
```

#### 圆形进度条
- **外圈**: 灰色描边，透明度 0.3，线宽 6pt
- **进度圈**: 主色描边，线宽 6pt，圆角端点
- **旋转**: -90度 (从顶部开始)
- **动画**: `.easeInOut`, 值绑定到 `progress`

#### 状态图标
- **Draft**: `doc.text`
- **Analyzing**: `brain.head.profile`
- **Generating**: `sparkles`
- **Completed**: `checkmark.circle.fill`
- **Failed**: `exclamationmark.triangle.fill`

#### 状态文本
- **内容**: `status.rawValue`
- **字体**: 17pt, Semibold
- **颜色**: `.primary`

#### 进度消息
- **内容**: 根据状态显示不同消息
- **字体**: 17pt, Regular
- **颜色**: `.secondary`
- **对齐**: 居中
- **内边距**: 水平 40pt

**进度值映射**:
- Draft: 0.0
- Analyzing: 0.3
- Generating: 0.7
- Completed: 1.0
- Failed: 0.0

---

### 6. 沉浸式 3D 视图 (ImmersiveView)

#### 布局结构
```
┌─────────────────────────────────────┐
│      RealityView (全屏)             │
│  ┌───────────────────────────────┐ │
│  │  3D 场景                      │ │
│  │  - 环境光                     │ │
│  │  - 3D 模型 (位置: [0,0,-1.5]) │ │
│  │  - 粒子效果 (条件)            │ │
│  └───────────────────────────────┘ │
│                                      │
│  ┌───────────────────────────────┐ │
│  │  控制面板 (底部覆盖层)         │ │
│  │  - 梦境信息卡片                │ │
│  │  - 控制按钮组                  │ │
│  │  - 退出按钮                    │ │
│  └───────────────────────────────┘ │
└─────────────────────────────────────┘
```

#### 3D 场景

**环境光**:
- **主光源**: DirectionalLight
  - 强度: 1000
  - 颜色: 白色
  - 位置: [0, 2, 2]

- **补充光源**: DirectionalLight
  - 强度: 500
  - 颜色: RGB(0.2, 0.5, 0.9)
  - 位置: [0, -1, 0]

**3D 模型**:
- **位置**: [0, 0, -1.5] (用户前方 1.5 米)
- **初始缩放**: [1.0, 1.0, 1.0]
- **旋转动画**: 30秒一圈，Y轴旋转
- **交互组件**:
  - `InputTargetComponent`: 接收输入事件
  - `CollisionComponent`: 碰撞检测
  - `HoverEffectComponent`: 悬停视觉反馈

**占位符场景** (条件显示):
- **显示条件**: 模型未加载或加载失败
- **内容**: 
  - 占位符球体 (半径 0.5)
  - 梦幻粒子效果 (50个粒子)

#### 手势交互

**1. 捏合缩放 (MagnifyGesture)**
- **范围**: 0.3x - 5.0x
- **响应**: 实时更新模型缩放
- **限制**: `min(max(newScale, 0.3), 5.0)`

**2. 旋转手势 (RotateGesture)**
- **轴**: Y轴 (垂直)
- **响应**: 实时更新模型旋转
- **累积**: 累加旋转角度

#### 控制面板 (底部覆盖层)

**梦境信息卡片**:
- **容器**: `LiquidGlassCard`, 内边距 20pt
- **内容**:
  - **标题**: Dream.title (22pt, Semibold)
  - **关键词**: 如果有分析结果，显示关键词列表
  - **解读**: 如果有分析结果，显示解读文本 (最多3行)
  - **控制按钮组**:
    - **Reset 按钮**: 
      - 图标: `arrow.counterclockwise`
      - 文本: "Reset"
      - 功能: 重置缩放和旋转
      - 动画: Spring (response: 0.3, dampingFraction: 0.7)
    - **Rotate 按钮**:
      - 图标: `arrow.triangle.2.circlepath`
      - 文本: "Rotate"
      - 功能: 旋转 90 度
      - 动画: Spring + Move (duration: 0.5s, easeInOut)

**退出按钮**:
- **组件**: `ToggleImmersiveSpaceButton`
- **位置**: 信息卡片下方
- **底部间距**: 40pt

**面板边距**: 水平 40pt

---

## 交互逻辑

### 导航流程

```
主界面 (ContentView)
├── Record Tab
│   └── DreamInputView
│       ├── 输入梦境
│       ├── 生成模型
│       └── (自动) → Dreams Tab (显示新梦境)
│
└── Dreams Tab
    └── DreamListView
        ├── 点击卡片 → DreamDetailView
        └── DreamDetailView
            └── 点击 "View in 3D Space" → ImmersiveView
```

### 状态转换

**梦境状态机**:
```
Draft → Analyzing → Generating → Completed
                              ↓
                           Failed
```

**状态持续时间**:
- Analyzing: ~5-10秒 (API 调用)
- Generating: ~30-120秒 (3D 生成 + 轮询)

### 错误处理

**错误类型**:
1. **API 错误**: 401 (认证失败), 429 (限流), 500+ (服务器错误)
2. **网络错误**: 连接超时、无网络
3. **解析错误**: JSON 解析失败
4. **模型加载错误**: USDZ 文件无效

**错误显示**:
- 位置: DreamInputView 底部
- 样式: 红色图标 + 错误卡片
- 交互: 可关闭

---

## 状态管理

### 全局状态

**DreamStore**:
- `dreams: [Dream]` - 所有梦境列表
- `currentDream: Dream?` - 当前处理的梦境
- `isLoading: Bool` - 加载状态
- `errorMessage: String?` - 错误消息

**AppModel**:
- `selectedDream: Dream?` - 选中的梦境 (用于沉浸式视图)
- `immersiveSpaceState` - 沉浸式空间状态
- `immersiveSpaceID` - 沉浸式空间 ID

### 本地状态

**DreamInputView**:
- `title: String` - 标题输入
- `description: String` - 描述输入
- `isRecording: Bool` - 录音状态
- `showError: Bool` - 错误弹窗显示
- `errorMessage: String` - 错误消息

**DreamListView**:
- `selectedDream: Dream?` - 选中的梦境
- `showDetail: Bool` - 详情页显示

**ImmersiveView**:
- `modelEntity: Entity?` - 3D 模型实体
- `scale: Float` - 缩放值 (1.0)
- `rotation: Float` - 旋转角度 (0.0)
- `isModelLoaded: Bool` - 模型加载状态

---

## 动画规范

### 过渡动画

**导航切换**:
- 类型: Spring
- Response: 0.3
- Damping Fraction: 0.7

**按钮交互**:
- 悬停: 阴影增强 (0.15s, easeInOut)
- 按压: 缩放 0.98 (0.1s, easeInOut)
- 释放: 恢复 (0.1s, easeInOut)

**卡片悬停**:
- 阴影增强: 0.2s, easeInOut
- 边框高光增强: 0.2s, easeInOut

**标签悬停**:
- 缩放: 1.0 → 1.05 (0.15s, easeInOut)

### 图标动画

**Pulse 动画**:
- 图标: `moon.zzz.fill`, `moon.stars`
- 类型: `.pulse`, `.repeating`

**Bounce 动画**:
- 触发: 导航项选中
- 类型: `.bounce`

**旋转动画**:
- 3D 模型: 30秒一圈, `.linear`
- 按钮旋转: 90度, 0.5s, `.easeInOut`

### 进度动画

**圆形进度条**:
- 类型: `.easeInOut`
- 绑定: `progress` 值变化

---

## 无障碍支持

### VoiceOver
- 所有按钮和控件都有 `accessibilityLabel`
- 重要操作有 `accessibilityHint`
- 标题标记为 `.isHeader`

### 减少动画
- 尊重 `accessibilityReduceMotion`
- 动画自动禁用

### 动态类型
- 所有文本使用系统字体
- 支持系统字体大小调整

---

## 性能优化

### 异步加载
- 3D 模型异步加载
- API 调用在后台线程
- UI 更新在主线程

### 资源管理
- 临时文件自动清理
- 模型实体正确释放
- 音频会话正确配置

### 列表优化
- 使用 `LazyVStack` 延迟加载
- 关键词标签最多显示 5 个
- 描述文本最多 3 行

---

## 设计检查清单

- [x] 所有界面使用液态玻璃效果
- [x] 符合 visionOS HIG 规范
- [x] 支持手势交互
- [x] 完整的错误处理
- [x] 无障碍支持
- [x] 动画过渡流畅
- [x] 状态管理清晰
- [x] 性能优化到位

---

**文档版本**: 1.0  
**最后更新**: 2025-11-09  
**适用平台**: Apple Vision Pro (visionOS)

